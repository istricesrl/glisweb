<?php

    /**
     * stampa delle informazioni di debug
     *
     * Questo file serve solamente a stampare, se necessario, varie informazioni di debug.
     *
     *
     *
     * @file
     *
     */

    // debug
	// header('content-type: text/plain;charset=utf-8');
	// rksort( $cf );
	// rksort( $ct );
	// print_r( $cf );
	// print_r( $ct );
	// print_r( $ct['page'] );
	// print_r( $ct['img'] );
	// print_r( $ct['site'] );
	// print_r( $_SESSION );
	// print_r( $_REQUEST );
	// print_r( $_SESSION['filter'] );
	// print_r( $ct['session'] );
	// print_r( get_included_files() );
	// print_r( $arrayLibrerie );
	// print_r( $arrayConfigBaseModuli );
	// print_r( $arrayConfig );
	// print_r( $_REQUEST['__signup__'] );
	// print_r( $cf['speed'] );
	// print_r( $cf['privacy'] );
	// print_r( $ct['contatti'] );
	// print_r( $cf['auth']['permissions']);

    // NOTA a questo punto non hanno ancora girato le macro, quindi le stampe migliori
    // si fanno non da qui ma da /_src/_api/_pages.php

    // debug
    // echo 'OUTPUT';
