{# IMPORTAZIONE LIBRERIE #}
{% import '_lib/_default.twig' as cms %}
{% import '_lib/_form.twig' as frm %}
{% import 'lib/default.twig' as def %}

{# ESTENSIONE DELLO SCHEMA DI BASE #}
{% extends 'ext/main.twig' %}

{# BLOCCO PRINCIPALE DELLA PAGINA #}
{% block main %}
<!-- blocco {{ _self }}::main -->

{# MODULO PRINCIPALE #}
<section class="row flex-fill">
    <div class="col-md-12 d-flex flex-column">

        {# APERTURA DEL FORM #}
        {{ frm.openForm({ 'form': form, 'page': page, 'session': session, 'request': request }) }}

            <fieldset>
                <legend>dati generali <small>{{ legend }}</small></legend>

                <div class="form-group row">
                    <div class="col col-md form-col">
                        <span class="label-top">prodotto</span>
                        {{ frm.select({ 'field': { 'name': 'id_prodotto' }, 'source': { 'api': 'prodotti' }, 'request': request, 'form': form, 'page': page, 'pages': pages }) }}
                    </div>
                    <div class="col-12 col-md-2 form-col">
                        <span class="label-top">codice</span>
                        {{ frm.input({ 'field': { 'name': 'codice', 'type': 'text' }, 'request': request, 'form': form, 'page': page }) }}
                    </div>
                    <div class="col-12 col-md-6 form-col">
                        <span class="label-top">nome</span>
                        {{ frm.input({ 'field': { 'name': 'nome', 'type': 'text' }, 'request': request, 'form': form, 'page': page }) }}
                    </div>                    
                </div>
            </fieldset>

            <fieldset>
                <legend>descrizione e codifica</legend>

                <div class="form-group row">
                    <div class="col-12 col-md-6 form-col">
                        <span class="label-top">note</span>
                        {{ frm.textarea({ 'field': { 'name': 'note', 'rows': 5 }, 'request': request, 'form': form, 'page': page }) }}
                    </div>
                    <div class="col-12 col-md-6 form-col">
                        <span class="label-top">note di codifica</span>
                        {{ frm.textarea({ 'field': { 'name': 'note_codifica', 'rows': 5 }, 'request': request, 'form': form, 'page': page }) }}
                    </div>
                </div>

            </fieldset>

            <fieldset>
                <legend>produttore</legend>

                <div class="form-group row">
                    <div class="col-12 col-md-6 form-col">
                        <span class="label-top">codice produttore</span>
                        {{ frm.input({ 'field': { 'name': 'codice_produttore', 'type': 'text' }, 'request': request, 'form': form, 'page': page }) }}
                    </div>
                    <div class="col-12 col-md-6 form-col">
                        <span class="label-top">EAN</span>
                        {{ frm.input({ 'field': { 'name': 'ean', 'type': 'text' }, 'request': request, 'form': form, 'page': page }) }}
                    </div>
                </div>
                
            </fieldset>

            {# BOTTONI E COMANDI DEL MODULO #}
            <fieldset class="form-controls mt-auto">
                {{ def.controls( page, pages, ietf, session, form.table, request ) }}
            </fieldset>

        </form>
    </div>
</section>

<!-- fine blocco {{ _self }}::main -->
{% endblock main %}
